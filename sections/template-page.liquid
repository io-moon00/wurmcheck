<div class="w-full flex items-center py-6 md:justify-start">
    <div class="flex flex-col max-w-6xl mx-auto w-full">
        <h1>{{ page.title }}</h1>
        <div class="max-w-none">
        {{ page.content }}
        </div>
    </div>
</div>